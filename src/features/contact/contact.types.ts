export type ContactMode = "briefing" | "quote";

export type ContactBriefingForm = {
    budget: string;
    companyName: string;
    contactName: string;
    deadline: string;
    email: string;
    objective: string;
    references: string;
    scope: string;
};
